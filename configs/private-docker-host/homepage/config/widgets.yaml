# Explicit empty override. Homepage ships a demo widgets.yaml (CPU/memory/
# disk resource bar + search box) baked into the image and falls back to it
# when this file is absent. Not needed for a launcher page - keeps it to just
# the service tiles.
[]
