upstream unifi-controller {
    server 10.1.0.3:8080;
}

server {
    listen                      8080;
    listen                      [::]:8080;
    proxy_pass                  unifi-controller;
    proxy_connect_timeout       5s;
    proxy_timeout               1800s;
}
