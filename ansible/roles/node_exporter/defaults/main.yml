---
# Allowlist for the systemd collector (systemd hosts only); it emits a series
# per unit per state. Uses [.] not \. -- systemd mangles escape sequences in ExecStart.
node_exporter_systemd_units: "(bitcoind|electrs|pve-cluster|pvedaemon|pveproxy|pvestatd|pve-firewall|ssh|uosserver)[.]service"
